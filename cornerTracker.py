import numpy as np
import cv2

def track_corners(corners: np.ndarray, car_velocity: int = 50, car_angle: int = 0):


    tracked_corners = []

    for image in corners:
        # Find corresponding corners
        for corner in image:
            break
            #print(corner)

    return tracked_corners